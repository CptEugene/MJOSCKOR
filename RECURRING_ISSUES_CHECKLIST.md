# MAYDAY Recurring Issues Checklist

이 문서는 반복적으로 재발했던 문제를 추적하기 위한 체크리스트다.
수정 전에는 관련 항목을 먼저 확인하고, 수정 후에는 아래 항목이 다시 깨지지 않았는지 확인한다.

## 날짜별 재발 기록

### 2026-03-23

- [ ] 슬롯 이름이 `No Commander` 등 기본값으로 되돌아감
- [ ] 주파수가 달라도 들리거나 오버레이에 뜸
- [ ] 저장 후 `client.toml` 파싱 오류 발생
- [ ] 단일 키인데 앞에 `+`가 붙음
- [ ] 장치 목록에 불필요한 장치가 다시 늘어남
- [ ] 종료 후 프로세스가 남음
- [ ] 오버레이가 실제 수신과 무관하게 전체 발화를 표시함
- [ ] 슬롯 중복 점유가 화면상 또는 서버상 허용됨
- [ ] 저장 팝업/접속 팝업이 안 뜨거나 크기가 이상함
- [ ] 마이크 레벨 측정이 자동으로 돌거나 PTT와 충돌함

### 새 재발 기록 템플릿

복사해서 추가:

```md
### YYYY-MM-DD

- [ ] 재발한 문제 1
- [ ] 재발한 문제 2
```

## 1. 슬롯 / 플릿트리

- [ ] 슬롯 더블클릭 시 트리가 전체 접히지 않는가
- [ ] PTT를 눌러도 플릿트리가 접히지 않는가
- [ ] 트리 snapshot 이후 슬롯 이름이 기본값으로 되돌아가지 않는가
- [ ] 내 슬롯 점유가 정상 표시되는가
- [ ] 다른 유저가 슬롯 이동 시 이전 슬롯 흔적이 남지 않는가
- [ ] 같은 슬롯에 다른 유저가 중복으로 들어갈 수 없는가
- [ ] 슬롯 점유 실패 시 화면상으로도 실제로 안 들어간 상태가 유지되는가
- [ ] 슬롯 성공 조인 시 `Slotjoin.mp3`가 재생되는가
- [ ] 플릿트리 편집기에서 `Fleet / Wing / Squad` 이름이 잘리지 않는가
- [ ] 플릿트리 편집은 관리자 비밀번호 없이 열리지 않는가

## 2. 설정 저장 / 설정 팝업

- [ ] 설정 팝업 배경색이 메인 톤과 다르게 깨지지 않는가
- [ ] 설정 팝업 구조가 `프로필 / 사운드 설정 / 채널 설정`으로 유지되는가
- [ ] `CONNECT` 버튼은 프로필 페이지에만 있는가
- [ ] `SAVE`를 누르면 저장 완료 팝업이 뜨는가
- [ ] `CONNECT` 성공/실패 팝업 크기가 정상인가
- [ ] 팝업 글자색/배경색이 읽기 어려워지지 않았는가
- [ ] 저장 후 재실행 시 `client.toml` 파싱 오류가 없는가
- [ ] 저장값이 재실행 후 그대로 유지되는가
- [ ] 장치/볼륨/채널/주파수/PTT 설정이 실제 런타임에 반영되는가

## 3. 서버 접속 / 로그

- [ ] 서버 접속이 반복적으로 끊기지 않는가
- [ ] 잘못된 서버 주소 / 비밀번호 / 기본 닉네임 `user`일 때 정확한 실패 사유가 뜨는가
- [ ] 서버 로그가 `[날짜 시간] 메시지` 형식으로 출력되는가
- [ ] `PTT UP/DOWN` 스팸 로그가 없는가
- [ ] 서버 로그에 접속자의 IP가 함께 찍히는가
- [ ] HELLO 로그에 `session 2`, `session 3` 식 세션 번호가 노출되지 않는가
- [ ] 연결 에러가 `session N error` 대신 사용자 기준으로 남는가

## 4. 주파수 / 라우팅

- [ ] 같은 채널이어도 주파수가 다르면 서로 안 들리는가
- [ ] 같은 채널 + 같은 주파수면 트리가 달라도 서로 들리는가
- [ ] `SQUAD`, `HQ`, `ATC/SHIP`, `GENERAL` 각각이 자기 주파수 기준으로만 동작하는가
- [ ] 오버레이가 실제 수신된 발화만 표시하는가

## 5. 오디오 / 무전 효과

- [ ] TX/RX 시작/종료 효과음이 채널별로 정상 재생되는가
- [ ] `CH2`가 `CH2_start/end.wav`를 사용하는가
- [ ] 채널별 톤 차이가 의도대로 유지되는가
- [ ] 보이스가 찢어지거나 `트트트` 같은 파열음이 심하지 않은가
- [ ] 보이스가 시간이 지나며 묻히거나 작아지지 않는가
- [ ] WAV 효과음만 들리고 실제 목소리가 안 들리는 상황이 없는가
- [ ] 동시 송신 시 한쪽이 완전히 끊기지 않고 둘 다 들리는가
- [ ] 장시간 PTT 중 `start/end` 효과음이 반복 재생되지 않는가
- [ ] 마이크/전체 사운드/채널별 사운드 슬라이더가 실제 체감되게 반영되는가
- [ ] 좌/우/양측 패닝이 실제 스피커 출력에 반영되는가
- [ ] Opus/AGC 추가 이후 끊김이 심해지지 않는가

## 6. 마이크 레벨 측정

- [ ] 마이크 레벨은 `측정 시작`을 눌렀을 때만 올라가는가
- [ ] `측정 종료`를 누르면 `0%`로 돌아가는가
- [ ] 측정 중에도 클라이언트가 멈추거나 종료되지 않는가
- [ ] 측정 시작 후 PTT 송신과 충돌하지 않는가

## 7. 입력 / PTT 바인딩

- [ ] 단일 키 지정 시 앞에 `+`가 붙지 않는가
- [ ] 조합키일 때만 `CTRL+1` 같은 형태로 `+`가 표시되는가
- [ ] 저장 후 재실행해도 단일 키에 `+`가 붙지 않는가
- [ ] 마우스1은 PTT로 지정되지 않는가
- [ ] 마우스4/5 + 다른 키 조합이 정상 저장/인식되는가
- [ ] 숫자패드 키가 정상적으로 인식되는가
- [ ] 숫자패드 입력이 다른 기능으로 잘못 해석되지 않는가
- [ ] PTT 캡처 중 실제 송신이 돌아가며 클라이언트가 꺼지지 않는가

## 8. 장치 목록

- [ ] 입력 장치 목록이 Windows 실제 사용 장치 기준으로 정리되는가
- [ ] 출력 장치 목록이 Windows 실제 사용 장치 기준으로 정리되는가
- [ ] `Microsoft Sound Mapper`, `Primary Sound Driver`, `Wave`, `mirroring`, `SST` 같은 항목이 과도하게 노출되지 않는가
- [ ] 장치 변경 후 실제 스트림이 다시 열리는가

## 9. 성능 / 종료

- [ ] 프로그램 시작 시 심한 랙이 없는가
- [ ] 창 드래그 시 주기적으로 멈추는 느낌이 없는가
- [ ] 클라이언트를 꺼도 프로세스가 남지 않는가
- [ ] 입력 리스너가 종료 후 남아 있지 않는가
- [ ] 백그라운드 감시 로직이 UI 프리즈를 만들지 않는가

## 10. 오버레이

- [ ] 오버레이 위치가 왼쪽 중앙인가
- [ ] 오버레이는 실제로 수신한 송신자만 표시하는가
- [ ] 주파수 안 맞아서 못 들은 사람은 오버레이에 뜨지 않는가
- [ ] 표시 내용이 `초록불 / 채널 / 이름`만 보이는가
- [ ] 말하는 사람이 없으면 오버레이가 숨겨지는가

## 11. 빌드 / 배포

- [ ] `exe만`이 아니라 폴더 전체가 배포되는가
- [ ] 클라이언트 패키지에 `data/fonts`, `data/sound`, `data/bin/opus.dll`이 포함되는가
- [ ] 서버 패키지에 `data/server.toml`, `data/fleet_tree.txt`가 포함되는가
- [ ] `Mayday.exe`, `MaydayServer.exe` 이름과 아이콘이 정상인가
- [ ] 배포 exe 실행 시 CMD 창이 계속 깜빡이지 않는가
- [ ] 최신 패키지 폴더명과 실제 exe 수정 시간이 맞는가

## 12. 수정 후 최소 확인 순서

- [ ] 설정 저장
- [ ] 서버 접속
- [ ] 슬롯 점유
- [ ] 슬롯 중복 점유 차단
- [ ] PTT 단일키 / 조합키
- [ ] 채널별 주파수 분리
- [ ] 채널별 효과음
- [ ] 오디오 수신/송신
- [ ] 오버레이 표시 조건
- [ ] 종료 후 프로세스 정리

## 13. 우선 주의 항목

- [ ] 슬롯 이름이 기본값으로 되돌아가는 문제
- [ ] 주파수 달라도 들리는 문제
- [ ] 저장 후 `client.toml` 깨짐
- [ ] 단일 키 앞에 `+`가 붙는 문제
- [ ] 장치 목록에 불필요한 항목이 다시 늘어나는 문제
- [ ] 오버레이가 실제 수신과 무관하게 뜨는 문제
- [ ] 종료 후 프로세스가 남는 문제
