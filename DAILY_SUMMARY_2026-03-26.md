# DAILY SUMMARY 2026-03-26

## 현재 상태

오늘 마감 시점 기준으로, 클라이언트 보이스 송수신은 다시 정상 동작하는 상태까지 복구되었다.  
가장 마지막으로 확인된 핵심 결과는 다음과 같다.

- 다른 PC에서도 마이크 레벨 측정, PTT 클릭음, 송신, 수신이 정상 동작함
- 최신 클라이언트 빌드 기준으로 오디오 시작 실패 회귀를 제거함
- 전체 자동 테스트는 `76 passed`
- `ruff check` clean

이번 작업은 하루 동안 여러 번 방향을 조정했지만, 최종적으로는 `실사용 가능한 안정화 상태`까지 올리는 데 목적이 있었다.

## 오늘 진행 흐름

오늘은 크게 5개의 흐름으로 작업이 진행되었다.

1. 채팅/오버레이/관리 기능 확장
2. 보이스 끊김, 동시무전, PTT 앞뒤 잘림 문제 보강
3. SRS와의 구조 차이 분석 및 오디오 엔진 구조 개선
4. 다른 PC에서 발생한 오디오 장치/송신 회귀 추적
5. 배포 빌드와 실제 타 PC 테스트 반복

초반에는 채팅 오버레이, OST 재생, 중앙 미션 안내 텍스트, 관리자 유지, 관리자 채팅 명령 등 기능 추가가 먼저 진행되었다.  
중반에는 보이스 품질 문제를 줄이기 위해 패킷 메타데이터, 발화자별 버퍼, 지터 대응, limiter, 볼륨 곡선, SRS 기준 필터값 정리 등을 넣었다.  
후반에는 다른 PC에서만 보이스가 망가지는 원인을 추적했고, 최종적으로는 `MME 입력 경로에서 RawInputStream을 여는 플래그 문제`가 오디오 런타임 전체를 죽이고 있었다는 점을 확인하고 수정했다.

## 오늘 수정한 것

### 1. 채팅 및 오버레이

- 왼쪽 하단 채팅 오버레이 추가
- 채팅창 배경 제거, 입력창 반투명 처리
- `/` 키로 채팅 입력 열기, `Enter` 전송, `Esc` 닫기
- 닉네임 색상을 플릿트리 역할 규칙에 맞게 표시
- 채팅 수신 시 짧은 알림음 추가
- 오버레이는 `Star Citizen` 실행 중일 때만 동작하도록 제한
- 화면 중앙 미션 안내 텍스트 오버레이 추가
  - 투명 배경
  - fade in / hold / fade out
  - 관리자 채팅 명령으로 송출 가능

### 2. 관리자 기능 및 제어

- 관리자 로그인 상태를 disconnect 전까지 유지
- `/play`, `/stopmusic`, `/텍스트`, `/텍스트g` 관리자 채팅 명령 추가
- 중앙 안내 텍스트, OST 재생 제어를 채팅 명령으로 보낼 수 있게 구성
- 서버에서 관리자 권한 검증을 추가해 UI 우회로 관리자 기능을 쓰지 못하도록 수정

### 3. OST / 상황 음악 기능

- 클라이언트 로컬 `music` 폴더 기반 음악 재생 구조 추가
- 서버 제어 메시지로 전체 클라이언트에 동기화 재생
- 관리자 UI와 관리자 채팅 명령 둘 다 지원
- 선택한 스피커 장치로 재생되도록 연결

### 4. 플릿트리 / UI 안정화

- 플릿트리 ID 충돌 수정
- 슬롯/스쿼드/윙 추가 후 선택이 풀리는 문제 수정
- 관리자 편집창과 메인 플릿트리 모두 스크롤 가능하게 수정
- 점유 슬롯 진입 실패 팝업과 경고음이 한 번만 나오던 문제 수정
- 레벨 측정 시작/종료에 별도 버튼음 적용
- 버튼 효과음 전체 음량 50%로 낮춤

### 5. 보이스 전송 구조 개선

- 음성 패킷에 `packet_number`, `sent_at_ms` 추가
- 수신은 발화자별 버퍼 기준으로 정리하도록 변경
- 전역 한 큐 중심 구조에서 발화자별 playout 기준으로 정리
- PTT 시작 시 preroll, 종료 시 release tail 추가
- 적응형 지터 버퍼 추가
- 최종 limiter 추가
- 채널/마이크 볼륨 슬라이더를 체감상 더 크게 반응하도록 조정
- 동시무전 시 마지막에 같이 합산되도록 믹싱 구조 개선
- 오랫동안 말하지 않은 수신자도 바로 들을 수 있게 UDP 등록/keepalive 추가

### 6. SRS 비교 및 반영

- SRS ARC-210 기준 TX/RX 필터값을 현재 구조에 맞춰 반영
- SRS와 현재 구조의 차이를 다시 분석해,
  - 값
  - DSP 체인
  - 송수신 파이프라인
  - 오디오 백엔드
  를 분리해서 정리함
- TX 경로를 메인 이벤트 루프 의존에서 벗어나 `TX 전용 워커` 구조로 개선
- 네이티브 `opus.dll`, `speexdsp.dll`, native WASAPI host 실험 경로를 추가

### 7. 다른 PC에서만 보이스가 망가지던 문제 수정

최종적으로 가장 중요했던 수정이다.

- 다른 PC에서
  - 마이크 레벨 측정이 안 되거나
  - PTT 클릭음이 안 나고
  - 송신이 안 나가고
  - 오디오 런타임 자체가 죽는 문제를 확인함
- 로그 확인 결과 `MME` 경로에서 `RawInputStream` 생성 시 `Invalid flag [PaErrorCode -9995]`가 발생하고 있었음
- 이 때문에 마이크 문제처럼 보였지만 실제로는 `오디오 런타임 시작 전체 실패`였음
- 수정 후에는 `WASAPI` 전용 시도와 `plain` 시도를 분리하고, `MME/기타 경로`에서는 문제 플래그 없이 열도록 변경함

이 수정으로 인해 최종 빌드에서 다른 PC 보이스 테스트가 다시 정상 동작하는 상태까지 회복되었다.

## 오늘 새로 만든 것

대표적으로 새로 추가되거나 실질적으로 새 기능으로 만든 구성은 다음과 같다.

- 채팅 오버레이 시스템
- 중앙 미션 텍스트 오버레이 시스템
- OST 재생 서비스 및 관리자 제어 경로
- 관리자 채팅 명령 파서
- 발화자별 오디오 버퍼
- 출력 limiter
- 네이티브 WASAPI 오디오 호스트 실험 경로
- 네이티브 오디오 엔진 브리지

## 오늘 확인된 중요한 사실

- `값만 SRS와 같다고 해서 실제 동작이 SRS와 같아지는 것은 아니다`
- 진짜 차이는 오디오 처리 구조, 실시간 경로, 장치 백엔드에서 발생했다
- 다른 PC에서 보이스가 망가진 문제는 서버가 아니라 `클라이언트 입력 스트림 시작 실패`였다
- `마이크 레벨 측정이 된다/안 된다`와 `실제 송신이 된다/안 된다`는 완전히 같은 단계가 아니었다
- 로그를 통해 회귀 원인을 잡는 흐름이 중요했고, 최종 원인은 코드로 확인되었다

## 내일 테스트해야 할 것

### 1. 보이스 회귀 테스트

- 기본 장치 선택 상태에서 레벨 측정, PTT 시작/종료, 실제 송신 확인
- 특정 장치 직접 선택 상태에서 레벨 측정, PTT 시작/종료, 실제 송신 확인
- USB 마이크, 헤드셋 마이크, 외장 인터페이스 각각 최소 1회 이상 확인
- 5분 이상 무전 없이 있다가 다시 무전했을 때 상대방에게 바로 들리는지 확인

### 2. 품질 테스트

- 같은 채널 / 같은 주파수에서 2명 동시무전
- `squad` 수신 중 `HQ`도 동시에 들어오는 경우
- 긴 문장을 말했을 때 2~3초마다 짧게 끊기는 현상이 남아 있는지 확인
- PTT를 누르자마자 말했을 때 앞부분 잘림이 없는지 확인

### 3. UI / 기능 테스트

- `/` 채팅 열기, `Enter` 전송, `Esc` 닫기
- `/텍스트`, `/텍스트g`, `/play`, `/stopmusic` 관리자 명령 확인
- 중앙 안내 텍스트 fade in/out 확인
- OST가 선택한 스피커 장치로 재생되는지 확인
- `Star Citizen`이 꺼져 있을 때 오버레이가 안 보이는지 확인

### 4. 배포 테스트

- 다른 PC에 `dist/client` 폴더 전체 복사 후 실행
- `client.toml` 장치값이 `-1 / -1`로 초기화된 상태에서 시작되는지 확인
- 실행 후 `runtime/client/logs/audio.log`가 정상 생성되는지 확인

## 내일 보강해야 할 것

### 1. 오디오 로그 보강

- 실제로 어떤 host API와 샘플레이트로 붙었는지 더 명확히 남기기
- 실패 시 `장치 이름 / host / sample rate / block size / 예외`를 더 읽기 쉽게 남기기

### 2. 장치 선택 안정화

- 장치 이름, endpoint id, 인덱스 복원 흐름을 더 단단히 만들기
- 장치가 사라졌거나 번호가 바뀌었을 때 fallback 동작을 더 명확히 만들기

### 3. 네이티브 오디오 엔진 방향성 정리

- 오늘 만든 native host / native engine 경로는 코드상 준비가 되어 있지만, 아직 기본 경로로 올린 상태는 아님
- 내일은 이 경로를 바로 기본으로 올릴지,
- 아니면 현재 안정화된 legacy 경로를 유지하면서 native 쪽을 별도 실험 브랜치처럼 다듬을지 결정해야 함

### 4. 실기 기반 최종 튜닝

- 동시무전 시 피크가 과하게 튀지 않는지
- 평균 음량이 너무 작거나 크지 않은지
- 채널별 수신 볼륨 체감이 충분히 큰지
- 장시간 사용 후에도 CMD/백그라운드 프로세스 잔존 문제가 없는지

## 결론

오늘 작업은 우여곡절이 많았지만, 결국 가장 큰 목표였던 `다른 PC에서도 실제로 보이스가 정상 동작하는 상태`까지 다시 끌어올렸다.  
특히 마지막에 잡은 `MME 입력 스트림 시작 실패` 회귀 수정이 가장 중요했고, 이 수정으로 현재 빌드는 다시 실사용 가능한 수준으로 돌아왔다.

내일은 새로운 기능을 더 늘리기보다, 지금 만든 구조를 기준으로 `다른 환경에서도 계속 안정적으로 유지되는지`를 확인하는 날로 가는 것이 맞다.

## 2026-03-26 추가 메모

아침 확인에서 `/텍스트` 명령이 사라진 것처럼 보였던 문제는 실제로 관리자 채팅 명령 파서 문자열이 깨져 있었기 때문이었다.  
이 부분을 수정해서 다시 다음 명령을 정상 지원하도록 복구했다.

- `/텍스트 내용`
- `/텍스트g 내용`
- `/play 트랙명`
- `/stopmusic`

추가로 아래 항목도 반영했다.

- 프로그램 시작 시 아이콘 + 로딩바 스플래시 추가
- 프로그램 종료 시 disconnect 효과음 재생
- `soundtrack stop` roundtrip 테스트 추가

이 시점 이후 추가로 우선해야 할 작업은 다음 순서가 맞다.

1. `/텍스트`, `/텍스트g`, `/play`, `/stopmusic`을 실제 GUI 기준으로 한 번 더 확인
2. 시작 로딩 스플래시 체감 속도 확인
3. 종료 시 disconnect 효과음이 너무 짧게 끊기지 않는지 확인
4. 보이스 장시간 회귀 테스트
5. native 오디오 엔진을 바로 기본으로 올릴지 여부는 충분한 실기 확인 뒤 결정
